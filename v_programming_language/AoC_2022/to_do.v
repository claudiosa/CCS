/*
TO FIX IN THE FUTURE ...

mut half := 0
for l in lines {
    print('\n Input line: ')
    half = l.len/2
	for i in 0..half{
		print('${l[i].ascii_str()}')
	}
	print("--")
    for i in half..l.len{
		print('${l[i].ascii_str()}')
	
	}    

	/*
	println('${half} -- ${line}')
	
	for i in 0..half{
		left[i] << line[i]
	}
	//: j in half..l.len 
	for i in  0..half {
		right[i] = line[i+half]
	}
	*/
  */  

% Input data for the queens_aplai program
% In this case it is just a number, so pretty ridiculous to separate.
% But you'll really want to use this in 'other' cases ; )

queens_data(normal, 8).

queens_data(half, 4).

queens_data(sixfourth, 12).

queens_data(lucky, 7).

% You do not want to run this
queens_data(hell, 666).

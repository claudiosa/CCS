import cp.
%%forall(member(X,[1,2,3]),p(X)).
int1(V) =>   

   X :: 1..3,
   Y :: 1..3,
   V := X > Y.

main ?=>   
   int1(V),
   %printf("V = %b \n", V),
   write(V),
   fail.

main => printf("\n FIM ").
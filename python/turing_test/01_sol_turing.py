from os import remove
import sys
# print(sys.argv)


def calPoints(ops) -> int:
    """
    :type ops: List[str] - List of operations
    :rtype: int - Sum of scores after performing all operations
    """
    result = None

    return result


if __name__ == '__main__':
    print(sys.argv)
    line = sys.argv.pop(0)  # remove the first element
#    line = (sys.argv).remove(str(header))

    print("\n the input string: ", line)
    #line = input()
    ops = line.strip().split()
    print(ops)
#        print(calPoints(ops))

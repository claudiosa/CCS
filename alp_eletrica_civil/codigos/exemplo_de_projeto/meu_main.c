#include "minhas_funcoes.h"


int main(void){
   int vet[N];
   
   f_le(&vet[0]);
   f_imp(vet);
   printf("A soma do vetor eh: %d", f_calcula(vet));
   printf("\n Profs are humans !!!\n"); 
   return 1;
 }  
